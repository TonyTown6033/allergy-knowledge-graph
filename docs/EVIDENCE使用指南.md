# Evidence 完整功能使用指南

## 🎯 功能概述

现在系统支持完整的知识图谱关联：

```
PDF 文档
   ↓ AI 提取
观点 (Claims) ←→ 证据 (Evidence) ←→ 本体 (Ontology)
   ↓ 上传
Notion 知识图谱
```

### 三个数据库的关联关系

1. **Ontology (本体)** - 医学概念节点（IgE、过敏性鼻炎等）
2. **Claims (观点)** - 核心论点，关联到本体概念和证据
3. **Evidence (证据)** - 原文引用，支持观点

## 🧪 测试方式

### 方式 1：演示脚本（推荐入门）

使用内置的样本文本进行演示：

```bash
uv run python demo_evidence_workflow.py
```

**功能：**
- ✅ 从样本医学文本中提取观点和证据
- ✅ 展示提取结果（观点、证据、关联概念）
- ✅ 可选择是否上传到 Notion
- ✅ 无需 PDF，快速测试

**适合场景：** 第一次测试，了解整个流程

---

### 方式 2：处理单个 PDF（完整流程）

从实际 PDF 文件中提取并上传：

```bash
uv run python upload_with_evidence.py "download/过敏原及自身抗体相关指南共识/过敏原/中国儿童过敏原检测临床应用专家共识(2021版).pdf"
```

**功能：**
- ✅ 提取观点和证据
- ✅ 先上传证据到 Evidence 数据库
- ✅ 再上传观点到 Claims 数据库
- ✅ 自动建立 Claim → Evidence 关联

**适合场景：** 实际使用，处理具体文献

---

### 方式 3：手动测试 Evidence 功能

使用测试脚本添加示例证据：

```bash
uv run python test_evidence.py
```

**功能：**
- ✅ 创建 5 条预定义的示例证据
- ✅ 直接上传到 Notion Evidence 数据库
- ✅ 验证数据库配置是否正确

**适合场景：** 验证 Evidence 数据库是否正常工作

---

## 📊 完整工作流程

### 步骤 1：确保数据库列已添加

**Evidence 数据库需要的列：**
- Name (Title) - 已存在
- 内容 (Text)
- 来源标题 (Text)
- 来源链接 (URL)
- 页码 (Number)

**Claims 数据库需要的列：**
- Name (Title)
- 内容 (Text)
- 极性 (Select: 支持/反驳/中立)
- 关联概念 (Multi-select)
- 来源标题 (Text)
- 来源链接 (URL)

### 步骤 2：选择测试方式

**新手推荐：**
```bash
# 1. 先用演示脚本了解流程
uv run python demo_evidence_workflow.py

# 2. 再测试 Evidence 功能
uv run python test_evidence.py

# 3. 最后处理实际 PDF
uv run python upload_with_evidence.py "your_pdf_file.pdf"
```

### 步骤 3：在 Notion 中验证结果

上传成功后，在 Notion 中查看：

1. **Evidence 数据库**
   - 查看提取的证据条目
   - 每条证据包含原文引用和来源信息

2. **Claims 数据库**
   - 查看提取的观点
   - 每个观点应该有关联的概念和证据

3. **关联关系**
   - Claims 中的 `evidence_ids` 字段引用 Evidence
   - Claims 中的 `关联概念` 字段引用 Ontology 节点

---

## 🔍 示例输出

### 演示脚本输出示例

```
======================================================================
演示：完整的观点和证据提取流程
======================================================================

[步骤 1] 初始化提取器...
  ✓ 初始化完成

[步骤 2] 从文本中提取观点和证据...
----------------------------------------------------------------------
样本文本（前200字）:
过敏性鼻炎的诊断与治疗研究...
----------------------------------------------------------------------

  ✓ 提取完成
    观点数: 4
    证据数: 4

[步骤 3] 提取结果预览...
======================================================================

观点 1: IgE检测在过敏性鼻炎诊断中的价值
  内容: 血清特异性IgE检测在AR诊断中具有重要价值，阳性率达到85.2%...
  极性: 支持
  关联概念: ige, allergic_rhinitis

证据 1: 证据：IgE检测在过敏性鼻炎诊断中的价值
  内容: 在本研究的500例患者中，IgE阳性率达到85.2%...
  来源: 过敏性鼻炎的诊断与治疗研究（示例）
```

---

## 💡 技术细节

### AI 提取的数据结构

```json
{
  "claims": [
    {
      "title": "观点标题",
      "content": "观点详细内容",
      "polarity": "支持",
      "concepts": ["IgE", "过敏性鼻炎"],
      "evidence": "支持该观点的原文引用"
    }
  ]
}
```

### Notion 数据库关联

```
Evidence (证据)
  ↓ (通过 ID)
Claim (观点)
  - evidence_ids: ["evidence_page_id_1", ...]
  - mapped_nodes: ["ige", "allergic_rhinitis", ...]
  ↓ (通过节点 ID)
Ontology (本体)
```

---

## 🚀 下一步

测试完成后，你可以：

1. **批量处理文献**
   - 修改 `upload_with_evidence.py` 支持批量处理
   - 建立完整的文献知识库

2. **查询和分析**
   - 在 Notion 中按概念查询相关观点
   - 追溯观点的原文证据
   - 分析不同文献的观点差异

3. **扩展功能**
   - 添加页码自动识别
   - 支持多种文档格式
   - 实现观点之间的关联

---

## ❓ 常见问题

**Q: 为什么需要 Evidence？**
A: Evidence 提供可追溯性，每个观点都能追溯到原文，保证学术严谨性。

**Q: 一个 Claim 可以关联多个 Evidence 吗？**
A: 可以！`evidence_ids` 是一个列表，可以关联多条证据。

**Q: Evidence 和 Claim 的内容有什么区别？**
A: 
- Evidence: 原文引用，尽可能保持原文
- Claim: AI 总结的观点，更简洁和结构化

**Q: 如果 PDF 提取失败怎么办？**
A: 
1. 检查 OpenAI API 配额
2. 确保 PDF 是文本型而非扫描件
3. 查看错误信息进行调试
