# Notion 免费版用户 - 快速设置指南

## 🎯 适用场景

本指南适用于**没有购买 Notion 会员**的用户，需要在 Page 下创建嵌套数据库。

## 📝 设置步骤

### 第 1 步：在 Notion 中创建一个页面

1. 打开你的 Notion 工作区
2. 创建一个新的页面（可以命名为"过敏知识图谱"或任何你喜欢的名字）
3. 复制这个页面的链接：
   - 点击页面右上角的 `...` 菜单
   - 选择 **"Copy link"**（复制链接）
   - 会得到类似这样的链接：
     ```
     https://www.notion.so/My-Page-1234567890abcdef1234567890abcdef
     ```

### 第 2 步：添加 Integration 到页面

1. 在刚创建的页面中，点击右上角的 `...` 菜单
2. 选择 **"Add connections"**（添加连接）
3. 找到并选择你的 Notion Integration
   - 如果还没有创建 Integration，访问 https://www.notion.so/my-integrations 创建一个

### 第 3 步：运行自动创建脚本

使用复制的页面链接运行脚本：

```bash
uv run python create_notion_databases.py https://www.notion.so/My-Page-1234567890abcdef1234567890abcdef
```

或者只使用 Page ID（链接最后的32位字符）：

```bash
uv run python create_notion_databases.py 1234567890abcdef1234567890abcdef
```

### 第 4 步：验证创建结果

脚本会自动：
- ✅ 在你的页面下创建3个数据库：
  - **Claims (观点)** - 存储从文献中提取的观点
  - **Ontology (本体)** - 存储过敏知识本体
  - **Evidence (证据)** - 存储证据引用
- ✅ 配置所有必需的属性和选项
- ✅ 自动更新 `.env` 文件中的数据库 ID

运行完成后，刷新你的 Notion 页面，就能看到新创建的数据库了！

### 第 5 步：开始使用

```bash
# 1. 检查配置
uv run python main.py config

# 2. 同步知识本体到 Notion
uv run python main.py ontology sync

# 3. 上传已提取的观点
uv run python upload_results.py results.json
```

## 🎨 数据库结构预览

### Claims 数据库（观点）
- 标题
- 内容
- 极性（支持/反驳/中立）
- 关联概念
- 来源标题
- 来源链接

### Ontology 数据库（本体）
- 名称
- ID
- 英文名
- 类别（过敏原/疾病/症状/检查/治疗/生物标志物/其他）
- 别名
- 描述
- 父节点

### Evidence 数据库（证据）
- 标题
- 内容
- 来源标题
- 来源链接
- 页码

## ❓ 常见问题

### Q: 脚本提示"无法访问页面"怎么办？
A: 确保在第2步中正确添加了 Integration 到页面。重新检查：
   1. 页面右上角 `...` -> "Add connections"
   2. 选择你的 Integration

### Q: 如何查看数据库 ID？
A: 脚本运行完成后会显示所有数据库 ID，也可以在 `.env` 文件中查看。

### Q: 可以删除重建吗？
A: 可以！删除 Notion 中的数据库，重新运行脚本即可。

### Q: 已经有数据库了，想重新创建怎么办？
A: 可以创建一个新页面，使用新页面的 URL 运行脚本。

## 💡 提示

- 免费版在 Page 下创建的数据库功能完全一样，只是位置不同
- 所有数据库会整齐地显示在你指定的页面中
- 可以随时移动、重命名这些数据库
- Integration 权限需要手动添加到页面才能访问
