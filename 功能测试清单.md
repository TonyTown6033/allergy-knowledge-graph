# 过敏知识图谱 - 功能测试清单

## ✅ 已完成测试的功能

### 1. **Ontology (本体) 管理** ✓
- [x] 查看所有本体节点: `uv run python main.py ontology list`
- [x] 按类别查看: `uv run python main.py ontology list --category 过敏原`
- [x] 同步到 Notion: `uv run python main.py ontology sync`
- **状态**: 27个节点已成功同步到 Notion

### 2. **文本概念搜索** ✓
- [x] 在文本中查找概念: `uv run python main.py search "文本内容"`
- **功能**: 从本地本体中匹配文本中的医学概念

### 3. **PDF 提取 (基础)** ✓
- [x] 单个 PDF 提取: `uv run python main.py extract file.pdf`
- [x] 批量处理: `uv run python main.py batch directory/ -o output.json`
- **状态**: 已从23个PDF中提取133个观点

### 4. **Claims (观点) 上传** ✓
- [x] 上传到 Notion: `uv run python upload_results.py results.json`
- **状态**: 观点已成功上传到 Notion

### 5. **配置检查** ✓
- [x] 验证配置: `uv run python main.py config`
- **状态**: 所有配置项正常

---

## 🆕 新增功能 - Evidence (证据) 支持

### 6. **Evidence 功能** ⭐ NEW

#### 测试选项：

**选项 A: 快速演示（推荐）**
```bash
uv run python demo_evidence_workflow.py
```
- 使用内置样本文本
- 展示完整提取流程
- 可选择上传到 Notion

**选项 B: 测试 Evidence 数据库**
```bash
uv run python test_evidence.py
```
- 创建5条示例证据
- 验证数据库配置

**选项 C: 处理实际 PDF（完整流程）**
```bash
uv run python upload_with_evidence.py "path/to/your.pdf"
```
- 从 PDF 提取观点和证据
- 建立关联关系
- 上传到 Notion

---

## 📊 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      PDF 文档库                              │
│  • 过敏原指南                                                │
│  • 诊疗规范                                                  │
│  • 专家共识                                                  │
└────────────────┬────────────────────────────────────────────┘
                 │
                 ↓ AI 提取
┌────────────────┴────────────────────────────────────────────┐
│               本地提取结果                                    │
│  • Claims (观点)                                             │
│  • Evidence (证据)                                           │
│  • 关联的本体概念                                            │
└────────────────┬────────────────────────────────────────────┘
                 │
                 ↓ 上传并建立关联
┌────────────────┴────────────────────────────────────────────┐
│                  Notion 知识图谱                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Ontology    │  │   Claims     │  │  Evidence    │     │
│  │  (本体)      │←─│   (观点)     │←─│   (证据)     │     │
│  │  27 个概念   │  │  133+ 观点   │  │  原文引用    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 推荐测试顺序

### 新用户：

1. ✅ **查看本体**: `uv run python main.py ontology list`
2. ✅ **搜索概念**: `uv run python main.py search "IgE 过敏"`
3. 🆕 **演示 Evidence**: `uv run python demo_evidence_workflow.py`
4. 🆕 **测试 Evidence**: `uv run python test_evidence.py`
5. 🆕 **处理 PDF**: `uv run python upload_with_evidence.py "your.pdf"`

### 已熟悉用户：

直接使用完整流程：
```bash
uv run python upload_with_evidence.py "download/过敏原及自身抗体相关指南共识/过敏原/中国儿童过敏原检测临床应用专家共识(2021版).pdf"
```

---

## 📁 核心文件说明

| 文件 | 功能 | 用途 |
|------|------|------|
| `main.py` | 主程序入口 | 本体、提取、批处理 |
| `upload_results.py` | 上传观点（旧版） | 上传已提取的观点 |
| `upload_with_evidence.py` | 完整上传（新版） ⭐ | 观点+证据+关联 |
| `demo_evidence_workflow.py` | 演示脚本 | 快速了解 Evidence 功能 |
| `test_evidence.py` | 测试脚本 | 验证 Evidence 数据库 |

---

## 🔧 Notion 数据库配置

### 必需的列：

**Ontology:**
- Name, ID, 英文名, 类别, 别名, 描述, 父节点

**Claims:**
- Name, 内容, 极性, 关联概念, 来源标题, 来源链接

**Evidence:**
- Name, 内容, 来源标题, 来源链接, 页码

---

## 📈 当前数据统计

- **本体节点**: 27 个（8个类别）
- **已处理文档**: 23 个 PDF
- **提取观点**: 133 个
- **支持证据**: 根据新功能动态生成

---

## 🚀 下一步建议

1. **测试 Evidence 功能**
   - 运行 `demo_evidence_workflow.py` 了解流程
   - 处理实际 PDF 建立知识图谱

2. **扩展本体**
   - 在 `src/ontology.py` 中添加更多医学概念
   - 重新同步到 Notion

3. **批量处理**
   - 处理自免相关文档
   - 建立更完整的知识库

4. **数据分析**
   - 在 Notion 中创建视图
   - 按概念、来源、极性筛选观点
   - 追溯观点的原文证据

---

## 💡 使用技巧

- 使用 `uv run` 无需手动激活虚拟环境
- 所有命令都支持 `--help` 查看详细说明
- 查看 `EVIDENCE使用指南.md` 了解详细的 Evidence 功能
- 遇到问题先运行 `uv run python main.py config` 检查配置
